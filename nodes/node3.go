package main

/*
import (
	"fmt"
	"time"
)

func main() {
	node := NewNode(3, 3, "localhost:5003")
	node.StartServer()

	fmt.Printf("Starting server for node %d on port %s\n", node.node_id, node.port)
	time.Sleep(2 * time.Second)

	peers := map[int]string{
		1: "localhost:5001",
		2: "localhost:5002",
	}
	node.DialOtherNodes(peers)

	// Test requesting CS
	node.RequestCriticalSection()

	select {} // Keep running
}
*/
